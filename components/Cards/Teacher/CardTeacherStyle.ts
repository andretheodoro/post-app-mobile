
import { StyleSheet } from 'react-native';

const cardTeacherStyle = StyleSheet.create({
    name: {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 10,
    },
    fone: {
        fontSize: 15,
        color: '#888',
        marginLeft: 10,
    }
});

export default cardTeacherStyle;
